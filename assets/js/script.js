// create a start button that starts the timer and shows the first question
// - create start button, add eventListener on it
// - when timer hits 0, stop timer and alert user that no time remains, generate the score as it hits 0
// - if timer is > 0, we the timer to decrement
// - - if timer is > 0, user should be able to select an answer to the questions
// - - if user has selected an answer, display whether the answer is correct or not
// - - once the correct/incorrect answer has been displayed, move on to the next question

// create an array of objects for questions